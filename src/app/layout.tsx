import type { Metadata } from "next";
import "./globals.css";
import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";

export const metadata: Metadata = {
  title: "Pet Palace - Professional Pet Hotel & Boarding",
  description: "Professional pet hotel and boarding services for your beloved companions. Luxury accommodations, expert care, and peace of mind while you're away.",
  keywords: "pet hotel, pet boarding, pet care, animal accommodation, dog boarding, cat boarding",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="font-sans antialiased">
        <div className="min-h-screen flex flex-col">
          <Header />
          <main className="flex-grow">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  );
}
